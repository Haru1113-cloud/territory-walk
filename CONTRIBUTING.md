# テラウォークへようこそ

歩いた道を囲んで、自分の陣地にする散歩ゲーム（試作中）。
このガイドは「誘われて興味を持ってくれた人」向けの、最短で参加できる手順です。

---

## まず動かしてみる（コードを書かなくてもOK）

一番手っ取り早いのはこれ。今公開されているURLを開くだけです。

```
https://haru1113-cloud.github.io/territory-walk/
```

「テストモード」を選んで地図をタップすれば、囲む→領土になる、の流れがすぐ体験できます。
ここまでは何のセットアップも要りません。

---

## コードを触ってみたくなったら

### 1. 必要なものを用意する

- [Git](https://git-scm.com/)
- [Flutter](https://docs.flutter.dev/get-started/install)
- [Claude Code](https://code.claude.com/) （自分のClaudeアカウントでログインして使います。招待した人のアカウントを共有する必要はありません）

### 2. コードを手元にコピーする

```
git clone https://github.com/Haru1113-cloud/territory-walk.git
cd territory-walk
flutter pub get
```

### 3. 動かしてみる

```
flutter run
```

接続しているシミュレーターや実機でアプリが起動します。

### 4. Claude Codeで作業する

プロジェクトのフォルダで `claude` と打って起動すれば、`CLAUDE.md` に書かれた
このプロジェクトの方針（コーディングのルール、今何を作っているか）を
Claude Codeが自動的に読み込みます。何も特別な説明をしなくても、
「このアプリってどういう作りになってるの？」と聞けば答えてくれるはずです。

---

## 作業するときの軽いルール

がっちり決めすぎると窮屈になるので、最低限だけ。

- **触る前にひとこと連絡する**: 「〇〇のあたり触るね」くらいの一言でOK。
  同じ場所を同時にいじると面倒なことになりがちなので、それだけ避けられれば十分です。
- **作業は自分のブランチで**: `main` に直接コミットせず、
  `git checkout -b 自分の名前/やること` のような形で分けておくと事故りにくいです。
  例: `git checkout -b taro/badge-ui`
- **終わったらプルリクエストを出す**: GitHub上で「Pull requests」→「New pull request」。
  お互い簡単に見てからマージすれば、壊れたコードがそのまま入ることを防げます。

これ以上のルールは、実際に困ってから必要な分だけ増やせば大丈夫です。

---

## 今何を作っているか（現時点のスコープ）

- ✅ ソロで歩いて陣地を囲む機能
- ✅ 散歩の記録・振り返りカレンダー
- ✅ バッジ・実績システム
- 🚧 他の人の陣地が見られる機能（閲覧のみ、実装検討中）
- ⏸ 陣地の奪い合い（対戦要素）は今のところ保留

質問や「こんな機能欲しい」があれば、GitHubのIssueに書いてもらえると管理しやすいです。
