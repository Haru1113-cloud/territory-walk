# テラウォーク

歩いた軌跡で囲んだエリアが自分の「領土」になる散歩ゲーム。Flutter製、
iOS/Android対応（まずは片方で動けばよい）。

このファイルは常時読み込まれるため、概要とポインタのみを書く。
数式・コーディング規約・描画の詳細は各skillに分離してあるので、
そちらを直接読むこと（このファイルに重複して書かない）。

複数人で開発する場合は CONTRIBUTING.md の軽いルールに従うこと。

## 現在のスコープ

**ソロ版＋他ユーザーの領土閲覧・ランキング・週替わりチャレンジ・応援・
写真ログ。** 自分の散歩・記録・バッジはソロ完結だが、確定した領土は
Firebase(無料のSparkプランのみで運用)経由で他ユーザーにも見え、獲得面積の
合計でランキング表示・週替わりのチーム目標の進捗表示もされる。他ユーザーに
軽い「応援スタンプ」を送ることもできる。対戦（領土の奪い合い）・ログイン・
ユーザーアカウントは実装しない（ニックネームはアカウントなしで端末
ローカルに保存するだけ）。散歩記録には写真を添付できる(端末ローカル保存、
base64埋め込み)。詳細は`docs/DESIGN.md`6章を参照。

「輪を閉じる」ボタンは廃止した。スタート地点付近に戻る、または軌跡が
自分自身と交差したときに自動で輪を閉じる。

スコープの詳細・データモデル・アーキテクチャ判断の理由は
`docs/DESIGN.md` を参照。

## MVPの機能

1. 地図表示
2. GPSでの現在地追跡・軌跡描画
3. 輪が閉じたら内側を領土として塗り、確定させる
4. 獲得した領土の合計面積を表示・保持（ローカルストレージ程度の永続化）

## 参照用プロトタイプ

`prototype/territory-walk-prototype.html` — コア機能（GPS軌跡記録、輪が
閉じたときの領土確定、面積計算）をブラウザで検証済みの試作品。
Dart移植時はこのファイルの考え方を変えない。数式の詳細は
`.claude/skills/geo-math/SKILL.md` を参照。

## ディレクトリ構成

```
territory-walk/
├── CLAUDE.md                  このファイル
├── docs/DESIGN.md             設計書（データモデル・アーキテクチャ・スコープ）
├── prototype/                 検証済みプロトタイプ（参照用、変更しない）
├── lib/                       Flutterソースコード
└── .claude/
    ├── settings.json          権限設定
    ├── agents/                サブエージェント定義
    ├── skills/                詳細情報（必要な時だけ読む）
    └── commands/              スラッシュコマンド定義
```

## Skillへのポインタ（詳細はここを読む）

作業内容に応じて、該当するskillを読んでから着手すること。

- **距離計算・面積計算・輪の自動クローズ判定・GPSブレ除去フィルタの数式**
  → `.claude/skills/geo-math/SKILL.md`
  （`lib/geo/` を書く/直すとき、面積や輪のクローズ挙動を調査するとき）

- **状態管理の方針・フォルダ分割ルール・命名規則**
  → `.claude/skills/flutter-conventions/SKILL.md`
  （`lib/` に新しいファイルを追加する、置き場所に迷ったとき）

- **地図上の描画（軌跡・領土ポリゴン・配色・レイヤー構成）**
  → `.claude/skills/territory-rendering/SKILL.md`
  （`lib/widgets/territory_map.dart` を書く/直すとき）

## サブエージェント

- `.claude/agents/flutter-implementer.md` — Flutter実装（`lib/`以下）を
  専門に担当するエージェント。DESIGN.mdと上記3つのskillを読んだ上で
  実装する前提で書かれている。

## スラッシュコマンド

- `/build-mvp` — DESIGN.mdの仕様に沿ってMVP実装を再実行/差分修正する。

## コーディング方針（プロジェクト全体で厳守）

- 「動けばいい」ではなく、後から読んで理解できるシンプルさを優先する。
- 各ファイル冒頭に、そのファイルの役割を日本語コメントで書く。
- 幾何計算・GPS処理・描画処理は責務ごとにファイルを分離する
  （1ファイルに全部詰め込まない）。
- 迷ったら過剰実装よりシンプルな実装を選ぶ。今回のスコープにない機能
  （対戦・サーバー同期等）のための抽象化を先回りして作らない。
- コメントは「非自明なWHY」のみ書く。コードを読めば分かるWHATは書かない。
